# ────────────────────────────────────────────
# نظام إدارة طلبات الشراء — متغيرات البيئة
# انسخ هذا الملف إلى .env وعدّل القيم
# ────────────────────────────────────────────

# قاعدة البيانات (SQLite افتراضياً)
# DATABASE_URL=sqlite:///./database/purchase_requests.db

# مفتاح JWT السري (غيّره في الإنتاج!)
JWT_SECRET_KEY=CHANGE-ME-IN-PRODUCTION-2024

# مدة صلاحية التوكن بالساعات
JWT_EXPIRY_HOURS=24

# وضع التشغيل
FLASK_DEBUG=false

# الخادم
HOST=0.0.0.0
PORT=5000

# CORS (استخدم * للتطوير أو أضف النطاقات مفصولة بفاصلة)
CORS_ORIGINS=*
