# نظام إدارة طلبات الشراء — صرح القابضة

نظام ويب متكامل لإدارة طلبات الشراء مع سير عمل آلي (5 مراحل)، صلاحيات حسب الأدوار، وتخطي تلقائي للمعتمدين المزدوجين.

## 🏗️ البنية

```
purchase_request_app/
├── run.py                          ← نقطة التشغيل
├── .env.example                    ← قالب متغيرات البيئة
├── requirements.txt                ← الاعتماديات
│
├── backend/
│   ├── config.py                   ← إعدادات موحّدة (من .env)
│   ├── app.py                      ← Flask App Factory
│   ├── database.py                 ← SQLAlchemy Engine & Session
│   ├── models.py                   ← جميع الموديلات
│   ├── seed_data.py                ← بيانات أولية
│   ├── migrate_db.py               ← ترقية قاعدة البيانات
│   ├── services/
│   │   └── workflow_service.py     ← منطق الأعمال (خرائط + صلاحيات + تخطي)
│   ├── routes/
│   │   ├── auth.py                 ← تسجيل الدخول
│   │   ├── requests.py             ← إنشاء الطلبات
│   │   ├── workflow.py             ← سير العمل + الموافقات
│   │   ├── procurement.py          ← المشتريات
│   │   ├── admin.py                ← لوحة المشرف (API)
│   │   ├── notifications.py        ← الإشعارات
│   │   └── upload.py               ← رفع ملفات Excel
│   └── utils/
│       ├── auth.py                 ← JWT + RBAC
│       ├── notifications.py        ← إنشاء الإشعارات
│       ├── watchers.py             ← متابعو الطلبات
│       └── excel_parser.py         ← قراءة ملفات Excel
│
├── frontend/                       ← HTML + CSS + JS (Vanilla)
├── tests/                          ← pytest (26 اختبار)
├── docs/                           ← وثائق النشر والتعليمات
└── deploy/                         ← سكربتات وملفات النشر (Nginx)
```

## ⚡ التشغيل السريع

```bash
# 1. إنشاء بيئة افتراضية
python -m venv .venv
.venv\Scripts\activate        # Windows
# source .venv/bin/activate   # Linux/Mac

# 2. تثبيت الاعتماديات
pip install -r requirements.txt

# 3. إعداد متغيرات البيئة (اختياري)
copy .env.example .env        # ثم عدّل القيم

# 4. التشغيل
python run.py
```

يعمل على: `http://localhost:5000`

## 🔐 الحسابات الافتراضية

| المستخدم | كلمة المرور | الدور | القسم |
|----------|------------|-------|------|
| `admin` | `Admin@2024` | مدير النظام | الإدارة العامة |
| `manager_finance` | `Finance@24` | مدير (مالية + معتمد مالي) | مالية |
| `manager_bizdev` | `BizDev@24` | مدير | تطوير الأعمال |
| `manager_hr` | `HumanR@24` | مدير | موارد بشرية |
| `manager_exec` | `Exec@2024` | مدير (تنفيذي + آمر صرف) | تنفيذية |
| `requester_finance` | `Fin2024!` | موظف | مالية |
| `procurement_user` | `Procure@24` | مشتريات | المشتريات |

## 🔄 سير العمل

```
إنشاء طلب → المدير المباشر → المدير المالي → آمر الصرف → المشتريات → مكتمل
                 ↓                ↓               ↓
              تخطي تلقائي    تخطي تلقائي    تخطي تلقائي
           (إذا نفس الشخص)  (إذا نفس الشخص) (إذا نفس الشخص)
```

## 🧪 الاختبارات

```bash
python -m pytest tests/ -v
```

## 🔧 متغيرات البيئة

| المتغير | الوصف | القيمة الافتراضية |
|---------|-------|------------------|
| `DATABASE_URL` | مسار قاعدة البيانات | `sqlite:///./database/purchase_requests.db` |
| `JWT_SECRET_KEY` | مفتاح JWT السري | يجب تغييره في الإنتاج |
| `JWT_EXPIRY_HOURS` | صلاحية التوكن | `24` |
| `FLASK_DEBUG` | وضع التطوير | `false` |
| `HOST` | عنوان الخادم | `0.0.0.0` |
| `PORT` | منفذ الخادم | `5000` |