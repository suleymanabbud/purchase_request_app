# 🛡️ خطة تحسينات قاعدة البيانات + حماية الحالات

> تاريخ: 2026-02-17

---

## 🚨 المشكلة الحرجة: رجوع الطلبات المعتمدة

### السبب الجذري
عند تغيير كود Python مع `debug=True`:
1. Flask يُعيد التشغيل تلقائياً
2. `seed_data.py` → يحذف مستخدم `disbursement_exec` + commit
3. `Base.metadata.create_all()` يُعيد إنشاء الجداول
4. إذا حدث أي خطأ أو corruption → الطلبات تضيع أو تعود لحالة أولية

### الحل
1. ✅ حماية `seed_data.py` من التعديلات غير الآمنة
2. ✅ إضافة نسخ احتياطي تلقائي قبل أي migration
3. ✅ إضافة حماية في `migrate_db.py` ضد تغيير الحالات
4. ✅ إضافة فحص سلامة البيانات بعد كل تشغيل

---

## 📋 التحسينات المطلوبة

| # | التحسين | الأولوية | الحالة |
|---|---------|----------|--------|
| 1 | حماية الحالات + نسخ احتياطي تلقائي | 🔴 حرج | ⏳ |
| 2 | نقل التواقيع لجدول منفصل (DB 6) | متوسطة | ⏳ |
| 3 | حذف أعمدة الموافقة المكررة (DB 1) | متوسطة | ⏳ |
| 4 | Float → Numeric للمبالغ (DB 4) | منخفضة | ⏳ |
| 5 | حذف جدول account_types (DB 8) | منخفضة | ⏳ |
| 6 | إضافة FK constraints (DB 3) | متوسطة | ⏳ |

---

## التنفيذ التفصيلي

### الخطوة 1: حماية الحالات + نسخ احتياطي
- إضافة `backup_database()` يُنشئ نسخة `.bak` قبل أي migration
- إضافة `verify_data_integrity()` يتحقق من عدم تغير الحالات
- تنظيف `seed_data.py` من الحذف الخطير

### الخطوة 2-6: تحسينات هيكلية
- سيتم تطبيقها عبر migration script آمن مع rollback
